# /dev — Lancer un agent dev sur une tâche

Lis `agents/dev.md` et implémente la tâche : $ARGUMENTS

Si $ARGUMENTS est vide, liste les fichiers `tasks/todo-*.md` disponibles et demande laquelle implémenter.
