# todo-refacto-20260309-002 — MSW conditional code in component instead of mocks/browser.ts
**Priorite** : critique
**Fichiers concernes** : `vetolib-frontend/src/components/MSWProvider.tsx` (lines 5, 8)
**Violation** : CLAUDE.md rule "Zero code conditionnel if (process.env.NODE_ENV === 'development') dans les composants — uniquement dans src/mocks/browser.ts". The MSWProvider component contains process.env.NODE_ENV checks directly.
**Correction attendue** : Move the environment detection logic into `src/mocks/browser.ts` (e.g., export an `initMSW()` that is a no-op in production). MSWProvider should import and call `initMSW()` without any environment checks in the component itself.
**Critere** : [] grep -r "NODE_ENV\|isDev\|isProduction" vetolib-frontend/src/components/ --include="*.tsx" returns no results
