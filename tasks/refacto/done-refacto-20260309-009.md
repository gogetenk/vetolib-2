# todo-refacto-20260309-009 -- Corriger bugs fonctionnels Program.cs (Dashboard + DbInitializer)
**Priorite** : importante
**Fichiers concernes** : `src/backend/Vetolib.Api/Program.cs`
**Violation** :
1. `DashboardEndpoints.MapDashboardApiEndpoints()` n'est pas appele -- les routes /api/dashboard/* sont inaccessibles
2. `DbInitializer.MigrateAllAsync()` et `SeedAsync()` ne sont pas appeles -- la DB n'est pas initialisee au demarrage
**Correction attendue** :
1. Ajouter `app.MapDashboardApiEndpoints();` dans Program.cs apres les autres MapXxxEndpoints()
2. Ajouter apres `var app = builder.Build();` :
```csharp
await DbInitializer.MigrateAllAsync(app.Services);
if (app.Environment.IsDevelopment())
    await DbInitializer.SeedAsync(app.Services);
```
**Critere** : Les endpoints /api/dashboard/stats repondent. En dev, les users demo sont crees automatiquement.
