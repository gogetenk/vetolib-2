# todo-refacto-20260309-001 — Direct fetch() in lib/api/billing.ts bypasses apiFetch client
**Priorite** : critique
**Fichiers concernes** : `vetolib-frontend/src/lib/api/billing.ts` (line 105)
**Violation** : All API calls must go through `lib/api/client.ts` (apiFetch). Direct `fetch()` in lib/api/ modules bypasses auth token injection, auto-refresh on 401, and error handling.
**Correction attendue** : Replace the raw `fetch()` call in `downloadInvoicePdf()` with a dedicated `apiFetchBlob()` method in `lib/api/client.ts` that handles authentication and returns a `Blob`. The client module should be the single point of contact for all HTTP calls.
**Critere** : [] grep -r "fetch(" vetolib-frontend/src/lib/api/ --include="*.ts" | grep -v "client.ts" returns no results
