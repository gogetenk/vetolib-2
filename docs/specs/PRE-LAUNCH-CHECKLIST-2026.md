# Pre-Launch Audit Checklist -- Vetara (2026-03-22)

Generated by automated code audit. Each item is marked PASS, FAIL, or WARN.

---

## 1. BRANDING -- "Vetolib" remnants in frontend

### User-visible source code

| Status | File | Detail |
|--------|------|--------|
| WARN | `src/components/features/landing/ExitIntentPopup.tsx:17` | Cookie name `vetolib_exit_intent_dismissed` -- not user-visible but leaks in DevTools |
| WARN | `src/components/features/landing/CompetitiveTableSection.tsx` | Interface field named `vetolib` (lines 10, 20, 82, 103) -- internal TS, not rendered as text |
| WARN | `src/components/features/onboarding/SetupChecklist.tsx:12` | LocalStorage key `vetolib-checklist-dismissed` -- not user-visible but leaks in DevTools |
| WARN | `src/lib/api/messaging-types.ts:3` | Code comment referencing `Vetolib.Messaging.Contracts` -- internal, harmless |
| PASS | i18n keys `messages/{en,fr,ar}.json` | The key is named `vetolib` but maps to `"Vetara"` / `"فيتارا"` -- correct |

### Non-source / tooling (acceptable)

- `package.json` / `package-lock.json`: npm package name is `vetolib-frontend` -- internal, never shown to users.
- `e2e/reports/ux-review-*.md`: QA reports reference old branding -- documentation only, not shipped.
- `playwright.*.config.ts`, `e2e/integration/global-setup.ts`: developer comments referencing `Vetolib.Api` -- backend project names, not user-facing.

### Verdict: PASS (no user-visible "Vetolib" text ships to production)

Minor cleanup recommended: rename cookie `vetolib_exit_intent_dismissed` to `vetara_exit_intent_dismissed` and localStorage key `vetolib-checklist-dismissed` to `vetara-checklist-dismissed` before GA.

---

## 2. PRICING COHERENCE

### EN (AED -- UAE market)

| Plan | Monthly | Annual | Status |
|------|---------|--------|--------|
| Starter | AED 299 | AED 254 | PASS |
| Professional | AED 449 | AED 382 | PASS |
| Enterprise | AED 649 | AED 552 | PASS |

### AR (AED -- UAE market)

| Plan | Monthly | Annual | Status |
|------|---------|--------|--------|
| Starter | 299 د.إ | 254 د.إ | PASS |
| Professional | 449 د.إ | 382 د.إ | PASS |
| Enterprise | 649 د.إ | 552 د.إ | PASS |

### FR (EUR -- France market)

| Plan | Monthly | Annual | Status |
|------|---------|--------|--------|
| Starter | 69 EUR | 59 EUR | PASS (different currency, intentional) |
| Professional | 99 EUR | 84 EUR | PASS |
| Enterprise | 149 EUR | 127 EUR | PASS |

### Landing page hardcoded values

- `src/app/[locale]/page.tsx:78-79` uses `lowPrice: "299"`, `highPrice: "649"` -- PASS (matches Starter/Enterprise monthly)

### "Free plan" mentions: **PASS** -- zero occurrences found.

---

## 3. TRIAL DURATION COHERENCE

### i18n message files: PASS

All trial mentions in `messages/en.json`, `messages/ar.json`, and `messages/fr.json` consistently say **30-day**.

### FAIL -- Help center hardcoded content

| File | Line | Problem |
|------|------|---------|
| `src/app/[locale]/help/page.tsx` | 283 | Says "14-day free trial" -- should be 30-day |
| `src/app/[locale]/help/page.tsx` | 291 | Says "14-day free trial" -- should be 30-day |

**Action required**: Change both occurrences of "14-day" to "30-day" in the help center FAQ content.

---

## 4. LINKS AND EMAILS

### vetara.com references

The domain `vetara.com` does not yet resolve. The following files reference it:

| File | Line | URL |
|------|------|-----|
| `src/app/sitemap.ts` | 4 | `https://vetara.com` (BASE_URL) |
| `src/app/robots.ts` | 12 | `https://vetara.com/sitemap.xml` |
| `src/app/[locale]/blog/[slug]/page.tsx` | 27, 69, 76, 88 | `https://vetara.com/en/blog/...`, `https://vetara.com/logo.png` |
| `src/components/features/portal/PortalLayout.tsx` | 118 | `https://vetara.com` |

**Action required before go-live**: DNS must be configured for `vetara.com`. These URLs are correct in structure but will 404 until the domain is live.

### Email addresses

| Email | Location | Status |
|-------|----------|--------|
| `support@vetara.ae` | `help/page.tsx` (lines 378, 870) | WARN -- must configure mailbox before launch |
| `hello@vetara.ae` | `page.tsx` (line 622), `PricingSection.tsx` (line 177) | WARN -- must configure mailbox before launch |
| `support@vetara.ae` | `page.tsx` (line 623) | WARN -- must configure mailbox before launch |

**Action required**: Verify `vetara.ae` domain is registered and email routing (MX records) is configured for `support@` and `hello@`.

---

## 5. DARK MODE CSS VARIABLES

### Analysis

Both light (`:root`) and dark (`.dark`) themes use oklch color space with hue ~195-200 (Deep Teal family). The palette is internally consistent:

**Light mode (`:root`)**:
- Primary: `oklch(0.47 0.12 195)` -- deep teal
- Background: `oklch(0.98 0.005 200)` -- near-white with slight teal tint
- Foreground: `oklch(0.18 0.02 200)` -- near-black

**Dark mode (`.dark`)**:
- Primary: `oklch(0.72 0.12 195)` -- lighter teal (correct inversion)
- Background: `oklch(0.15 0.015 200)` -- near-black with teal tint
- Foreground: `oklch(0.95 0.01 200)` -- near-white

All sidebar, card, popover, accent, muted, border, input, ring, and chart tokens are defined for both modes. Semantic colors (destructive, success, warning, info) have dark variants.

### Verdict: PASS -- Dark mode palette is complete and consistent with Deep Teal branding.

---

## 6. CONSOLE STATEMENTS IN PRODUCTION CODE

### `console.log` / `console.warn` / `console.debug` / `console.info`: PASS -- zero found in `src/`.

### `console.error`: 1 occurrence

| File | Line | Context |
|------|------|---------|
| `src/app/[locale]/(dashboard)/error.tsx` | 13 | `console.error(error)` -- inside Next.js error boundary |

### Verdict: PASS -- The single `console.error` is in a Next.js error boundary component, which is the standard pattern for error reporting. No debug logging remains.

---

## 7. ENVIRONMENT VARIABLES

### Required for deployment

| Variable | Required | Default | Purpose |
|----------|----------|---------|---------|
| `NEXT_PUBLIC_API_URL` | **Yes** (production) | `""` (empty = MSW mode) | Backend API base URL |
| `BACKEND_URL` | Optional | Falls back to `NEXT_PUBLIC_API_URL` | Server-side API URL (for Next.js API routes, can differ from public URL) |
| `NEXT_PUBLIC_POSTHOG_KEY` | Optional | `undefined` (analytics disabled) | PostHog analytics project key |
| `NEXT_PUBLIC_POSTHOG_HOST` | Optional | `https://eu.i.posthog.com` | PostHog ingestion endpoint |
| `NODE_ENV` | Auto | `production` in builds | Controls secure cookies, MSW activation |

### Existing env files

- `.env.local` -- development (MSW mode, no API URL)
- `.env.wire` -- integration testing (API URL = localhost:5295)
- `.env.example` -- **DOES NOT EXIST**

**Action required**: A `.env.example` file should be created for deployment documentation.

---

## Summary

| Category | Status | Blocking? |
|----------|--------|-----------|
| 1. Branding (Vetolib remnants) | PASS (minor cleanup recommended) | No |
| 2. Pricing coherence | PASS | No |
| 3. Trial duration | **FAIL** -- 2 occurrences of "14-day" in help center | **Yes** |
| 4. Dead links (vetara.com) | WARN -- domain not live yet | Yes (before DNS go-live) |
| 4b. Email addresses | WARN -- mailboxes not confirmed | Yes (before launch) |
| 5. Dark mode CSS | PASS | No |
| 6. Console statements | PASS | No |
| 7. Environment variables | WARN -- no `.env.example` | No (recommended) |

### Blockers before go-live

1. **Fix "14-day" to "30-day"** in `src/app/[locale]/help/page.tsx` (lines 283 and 291)
2. **Configure DNS** for `vetara.com` (sitemap, robots, blog canonical URLs, portal link)
3. **Configure email** for `vetara.ae` domain (`support@vetara.ae`, `hello@vetara.ae`)
4. **Set `NEXT_PUBLIC_API_URL`** in production environment to point to the real backend
